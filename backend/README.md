# Backend M1

Java 21 + Spring Boot 4.1.1 + Spring MVC + Spring Security + MyBatis + PostgreSQL + Redis + Flyway。

本阶段提供：统一响应、异常处理、Request ID、JWT 登录、M1 管理员初始化、Swagger、Actuator 和 Dashboard 联调接口。

默认开发账号：`admin / admin123`。仅用于本地 M1 验证，非开发环境必须通过环境变量覆盖。
