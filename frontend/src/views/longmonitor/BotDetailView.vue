<script setup lang="ts">
import { onMounted,ref } from 'vue'
import { useRoute,useRouter } from 'vue-router'
import { longMonitorApi } from '@/api/longMonitor'
const route=useRoute(),router=useRouter(),row=ref<any>({}),loading=ref(false),data=(r:any)=>r.data.data
async function load(){loading.value=true;try{row.value=data(await longMonitorApi.bot(Number(route.params.id)))||{}}finally{loading.value=false}}onMounted(load)
</script><template><div class="page"><div class="head"><div><h2>客户机器人详情</h2><p>敏感凭证仅显示是否已配置，不在页面回显明文。</p></div><el-button @click="router.back()">返回</el-button></div><el-card shadow="never" v-loading="loading"><el-descriptions :column="2" border><el-descriptions-item label="机器人编号">{{row.bot_no}}</el-descriptions-item><el-descriptions-item label="客户">{{row.customer_name}}</el-descriptions-item><el-descriptions-item label="名称">{{row.bot_name}}</el-descriptions-item><el-descriptions-item label="类型">{{row.bot_type}}</el-descriptions-item><el-descriptions-item label="Webhook">{{row.webhook_url}}</el-descriptions-item><el-descriptions-item label="启用">{{row.enabled?'是':'否'}}</el-descriptions-item><el-descriptions-item label="最近测试">{{row.last_test_status}} / {{row.last_test_at}}</el-descriptions-item><el-descriptions-item label="测试信息">{{row.last_test_message}}</el-descriptions-item><el-descriptions-item label="备注" :span="2">{{row.remark}}</el-descriptions-item></el-descriptions></el-card></div></template><style scoped>.page{display:grid;gap:16px}.head{display:flex;justify-content:space-between}.head h2{margin:0 0 6px}.head p{margin:0;color:#7b8494}</style>