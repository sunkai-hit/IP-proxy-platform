# Frontend M1

Vue 3 + TypeScript + Vite 8 + Element Plus + Pinia + Vue Router + Axios。

M1 提供登录页、控制台 Layout、真实 Dashboard 联调页和后续业务模块路由占位。

本地开发：`npm install && npm run dev`，Vite 会把 `/api` 代理到 `http://localhost:8080`。
